#include <stdio.h>

int main()
{
    int idade;

    printf("Digite sua idade: ");
    scanf("%d", &idade);

    if (idade <= 12) {
        printf("Voce é criança");
    }
    else if (idade <= 17) {
        printf("Voce é adolescente");
    }
    else {
        ("Voce é adulto");
    }

    return 0;
}